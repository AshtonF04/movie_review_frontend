// API configuration
// In development, this will use localhost
// In production, this will use the environment variable set by Vercel/Netlify
export const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000/api';

